{% extends '_base.html.twig' %}

{% block main %}

    <ol style="columns:30ex auto">
        {% for p in database.query('SELECT * FROM pages WHERE template="street" ORDER BY title ASC') %}
            <li>
                <a href="{{page.link(p.id)}}.html">{{p.title}}</a>
            </li>
        {% endfor %}
    </ol>

{% endblock %}
